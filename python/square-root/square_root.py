from math import isqrt


def square_root(number: int) -> int:
    """Get the square root of the number.

    :param number: int - Number to get the square root.
    :return: int - Square root in integer.
    """
    return isqrt(number)
